name: Deploy

# Trigger the workflow on push and
# pull request events on the master branch
on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

# Authenticate to the the server via ssh
# and run our deployment script
jobs:
  deploy1:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Deploy to Server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSHKEY }}
          script: "cd /var/www/agent.innotraveltech.com && ./.scripts/deploy.sh && cd /var/www/nsn.innotraveltech.com && ./.scripts/deploy.sh && cd /var/www/apps.fasttravel.pk && ./.scripts/deploy.sh"

  deploy2:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Deploy to Server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOST2 }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSHKEY2 }}
          script: "cd /var/www/agent.airpass.tours && ./.scripts/deploy.sh"
  deployakij:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Deploy to Server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOSTAKIJ }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSHKEYAKIJ }}
          script: "cd /var/www/agent.akijair.com && ./.scripts/deploy.sh"
  deploybuyairticket:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Deploy to Server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOSTBUYAIRTICKET }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSHKEYBUYAIRTICKET }}
          script: "cd /var/www/agent.buyairticket.com && ./.scripts/deploy.sh"
  deploygofly:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Deploy to Server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOSTGOFLY }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSHKEYGOFLY }}
          script: "cd /var/www/agent.gofly.com.bd && ./.scripts/deploy.sh"
  deploygtr:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Deploy to Server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOSTGTR }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSHKEYGTR }}
          script: "cd /var/www/orbit.gtrsystem.com && ./.scripts/deploy.sh"
  deployhstrip:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Deploy to Server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOSTHSTRIP }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSHKEYHSTRIP }}
          script: "cd /var/www/agent.hstrip.net && ./.scripts/deploy.sh"
  deploymyvoiaj:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Deploy to Server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOSTMYVOIAJ }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSHKEYMYVOIAJ }}
          script: "cd /var/www/agent.myvoiaj.com && ./.scripts/deploy.sh"
  deploynakamura:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Deploy to Server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOSTNAKAMURA }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSHKEYNAKAMURA }}
          script: "cd /var/www/b2b.nakamura-tour.com && ./.scripts/deploy.sh"
  deployonewagent:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Deploy to Server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOSTONEAGENT }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSHKEYONEAGENT }}
          script: "cd /var/www/agent.oneagent.biz && ./.scripts/deploy.sh"
  deployeot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Deploy to Server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOSTEOT }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSHKEYEOT }}
          script: "cd /var/www/agent.easyonwardticket.com && ./.scripts/deploy.sh"
  deploysand:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Deploy to Server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOSTSAND }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSHKEYSAND }}
          script: "cd /var/www/sandbox-b2b.myvoiaj.com && ./.scripts/deploy.sh"
  deploysinbd:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Deploy to Server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOSTSINBD }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSHKEYSINBD }}
          script: "cd /var/www/agent.sinbadbd.com && ./.scripts/deploy.sh"
  deploybiggo:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Deploy to Server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOSTBIGGO }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSHKEYBIGGO }}
          script: "cd /var/www/agent.biggo.travel && ./.scripts/deploy.sh"
  deployumrah:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Deploy to Server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOSTUMRAH }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSHUMRAH }}
          script: "cd /var/www/agent.umrahbooking.com && ./.scripts/deploy.sh"
